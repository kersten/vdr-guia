var entry = function () {
    
};

entry.prototype.hasActor = function (name) {
    
};

entry.prototype.hasCountry = function (name) {
    
};

entry.prototype.hasRegisseur = function (name) {
    
};

entry.prototype.hasYear = function (name) {
    
};

entry.prototype.getActors = function () {
    
};

entry.prototype.getCountry = function () {
    
};

entry.prototype.getRegisseur = function () {
    
};

entry.prototype.getYear = function () {
    
};